services:
  database:
    image: postgres:18.1-alpine
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - "POSTGRES_DB=jorken"
      - "POSTGRES_USER=admin"
      - "POSTGRES_PASSWORD=admin"